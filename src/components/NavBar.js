import React from "react";

function NavBar() {
  // update the JSX being returned!
  return (
  <div id="Nav">
    <nav>
    <a href="#Home">Home</a>
    <a href="#About">About</a>
  </nav>
  </div>
  );
}

export default NavBar;
